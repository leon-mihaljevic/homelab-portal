pipeline {
    agent any
    stages {
        stage('Hello') {
            steps {
                echo 'Jenkins can see this repo -- pipeline skeleton is alive.'
                sh 'docker --version'
            }
        }
    }
}
